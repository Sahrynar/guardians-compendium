# PATCH 6 — Language Workshop + held queue (July 3, 2026)

## 🗣 LANGUAGE WORKSHOP (new tool, second in the Tools list)
The flavor generator, built to the spec you approved:
- 44 sound palettes across 10 groups — incl. your additions: Yoruba/Igbo/Hausa (1550s
  Nigeria), Ancient Egyptian, Latin, Sumerian, Proto-Semitic, PIE, and six Mesoamerican.
- Blend up to 4 ingredients with % dials · muddiness meter (🟢🟡🟠🔴) warns before soup.
- Soft↔Hard slider (defaults to your Venetian-70/Sicilian-30, softness 70 — Martyn's mix).
- Length auto/1–5 syllables · optional "translate by feel" seed word.
- 💾 SAVE PRESETS — and saved languages appear as INGREDIENTS for new mixes (your
  Murvetian-inside-another-blend idea works).
- ✨ SURPRISE ME — pulls from saved presets AND unsaved raw palettes, as requested.
- 📖 Palette Reference popout — the plain-language catalogue lives inside the tool.
- Every output: 🔊 (dominant palette's closest real voice) + copy. Nothing is auto-canon.
HONEST CAVEAT: the phonotactics are v1 — expect some soup until you tune mixes.
That's the "try it and see" round you asked for.

## Everything else from the held queue
- 🖼 From Library: Characters portrait now has it (Items & Inventory already had their own
  Browse Library — discovered wired by an earlier pass). FamilyTree bg + reference-image
  slots + preset slots = next pass.
- Import clarity: summary now reports "· N already present" — the "0 added" you saw was
  almost certainly re-imports of already-landed data; now it will SAY so.
- Glossary: XS–XL sizing chips + "+ Term" manual add/edit modal (edit via dropdown for now;
  inline ✎ per card comes with the card-polish pass). Saves as is_glossary wiki entries.
- session_log imports now UPDATE same-id entries — enriched logs can replace slim ones.
- sw → v25.

## Deliberately deferred to PATCH7 (each is half a patch alone)
Filters & sorters everywhere · global search unification.

## Verified
vite build ✓ (twice) · ESLint 0 ✓ · unicode scan 0 ✓
